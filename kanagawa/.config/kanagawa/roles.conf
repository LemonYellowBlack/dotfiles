# Kanagawa semantic roles — kitty format
# Source of truth: ~/lab/dotfiles/kanagawa-palette.md
#
# kitty doesn't support variables, so this file directly assigns the chrome
# keys. Included from kitty.conf via `include ~/.config/kanagawa/roles.conf`.

# focus accent (waveBlue2)
active_border_color     #2D4F67

# dim accent (sumiInk4)
inactive_border_color   #2A2A37

# chrome (sumiInk0)
tab_bar_background      #16161D
inactive_tab_background #16161D

# content (sumiInk3)
active_tab_background   #1F1F28
