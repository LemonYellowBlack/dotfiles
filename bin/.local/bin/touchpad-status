#!/bin/sh

FLAG="/tmp/touchpad-disabled"

if [ -f "$FLAG" ]; then
    notify-send -t 3000 \
        -h string:x-canonical-private-synchronous:touchpad \
        "Touchpad Off" ""
else
    notify-send -t 3000 \
        -h string:x-canonical-private-synchronous:touchpad \
        "Touchpad On" ""
fi
