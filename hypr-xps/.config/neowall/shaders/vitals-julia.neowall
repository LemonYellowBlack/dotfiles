# vitals.neowall: sidecar manifest for vitals.glsl.
# Buffer A reads its own previous frame (self) so it can smooth values and
# accumulate the moon's orbit phase. The Image pass reads Buffer A.
shader vitals.glsl

bufferA { ch0 self }
image   { ch0 bufferA }
